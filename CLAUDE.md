# CLAUDE.md

このファイルは Claude がセッションを跨いで文脈を引き継ぐためのメモです。
セッション開始時に必ず読み込まれます。重要な決定事項や覚えておくべきことが
出てきたら、Claude はこのファイルの「記憶メモ」に追記してコミットすること。

> ## 📦 案件の所在は `_memory/INDEX.md` を見ること
> 「あの案件どうなってた？」「〇〇の資料どこ？」と聞かれたら、**まず `_memory/INDEX.md`**。
> 全案件の進捗・成果物がどのブランチにあるかの地図になっている。
> セッション開始時にフック（`.claude/hooks/session-start.sh`）が自動で読み込む。
> **作業が一区切りしたら、成果物を push した上で INDEX を更新すること。**

## ユーザーについて

- 殿村亮太さん（DYM）。日本語で会話する。
- 返答は **短く・シンプルに・具体的な手順で**。長い解説や専門用語の羅列は好まない。
- セッションやデバイスを跨いでも「前に言ったこと」を覚えていてほしいという要望が強い。
  → だからこのファイルに要点を残す。会話で重要なことが決まったら必ずここに追記する。

## このリポジトリについて

- 営業提案資料（PPTX）の置き場 + 資料を自動生成するスクリプト群。
  - ルート直下: LINE公式アカウント活用提案などの完成デッキ（.pptx / .xlsx）
  - `_templates/`: デッキのベースとなるテンプレート PPTX
  - `_build/`: テンプレートから提案デッキを生成する Python スクリプト
    （`build_vivical_33.py`, `build_vivical_9p.py`, 検品用の `qa_render.py`）
  - `.claude/skills/`: このリポジトリで使えるスキル（Claude Code が自動で読む）
- 認証情報類（*.json, .env など）は絶対にコミットしない（.gitignore 参照）。

## ★禁止事項（全セッション共通・最優先で守る）

- 2026-09-02: **PowerPoint / Excel / Word のプロセスを絶対に落とさない。**
  PC側のセッションで、ファイルが開けない（ロック）→ `taskkill` で POWERPNT.EXE を
  強制終了、を**3回やった**。殿村さんが作業中の画面を落としている。
  → ファイルがロックされていたら**「〇〇を閉じてください」と頼んで待つ**。
    別名で保存する・コピーを作るのも可。プロセスを殺す選択肢は無い。
- 2026-08-31: **頼まれていない物を先に作って渡さない。**（行を足すGASで795行ずらした事故）
  列名を言われたら、その並びで1行=1レコードを出すのが先。集計・数式・GASは頼まれてから。
- 2026-08-31: **実データを見ずに列の意味を推測して埋めない。**
- 2026-08-31: **人が使っている共有シートには、頼まれるまで書き込まない。**

## 記憶メモ（会話を跨いで覚えておくこと）

- 2026-08-24: クロスデバイスの記憶について整理した。
  - claude.ai アプリ（スマホ/デスクトップ/Web）の会話は「メモリ機能」で
    アカウント単位に自動共有される（設定 → 機能 → メモリ。反映に最大1日ラグ）。
  - Claude Code のセッションは自動同期されないため、覚えておくべきことは
    この CLAUDE.md に書いてコミットする、というのがこのリポジトリでの運用。

- 2026-08-26: スキル `lineoa-industry` の使い方。
  - 実体は `.claude/skills/lineoa-industry/`（配布版は `skills/lineoa-industry.skill`）。
  - 使い方：チャットに「ブライダル業界」等と業界名だけ送る。PPTXが要るときは
    「pptxで」「資料まで」、事例集は「事例集」と一言添える。
  - 3モード：A=15スライドの提案原稿テキスト（既定）／B=業界汎用PPTX 39枚（2ラリー）／
    C=LINEヤフー公式の導入事例集PPTX。
  - 本文中で参照している関連スキルのうち `dym-format` / `zengo-search` /
    `lineoa-pre-visit-research` は未設置。必要になったら同じ場所に追加する。
    ※ `lineoa-sim` は**未設置ではなかった**（別ブランチにあった）。2026-09-01に集約済み。

- 2026-08-26: 注文住宅業界のモードB資料を作った（ルート直下に納品済み）。
  - **引き継ぐときは `_drafts/注文住宅業界_要件定義（引き継ぎ用）.md` を最初に読ませること。**
    前提・36枚の構成・未完タスク・判断保留の論点・ビルド手順が全部入っている。
  - `注文住宅業界_LINEOA施策提案.pptx`（36枚）／`注文住宅業界資料_公表判断チェックリスト.xlsx`
  - 再生成は `_build/build_chumon_jutaku.py`（文言修正はここを触る）と `build_checklist.py`。
  - **未完の申し送り2件：**
    1. **チェックリストが赤4枚（S18/S24/S25/S29）。** FMT由来の出典未記載の数値
       （「業態平均で最大3%」「CVR 2%→5-10%」「商談化70%改善」「友だち追加率0.8-1.0%」）。
       出典を社内で確定するか削除するまで**対外提出不可**。→ 殿村さんが確認中。
    2. **競合の友だち数が未取得**（S9/S10 は破線の差込枠のまま）。クラウド環境からは
       `page.line.me` / `lineoa.jp` が egress ブロックで見られない。**PC側で取得すること。**
       同じ理由で Googleトレンド画像・LINEヤフー前後検索データも未反映（S7/S8 は推計・仮説）。
  - **論点：この資料は「LINEOA運用」の話。広告の扱いに注意。**
    S5（検索広告CPC推移）とS6（広告実績CVR＋広告審査）は Web広告全般の話で、
    スキル §6.5 の最上級FMT構成に指定があるから入れている。運用に純化したいなら
    この2枚が削除候補（LINE広告・CPFは友だち追加動線なので残す）。→ 判断保留中。
  - モードAの原稿とLINE公式の導入事例リサーチは `_drafts/` に置いた。
    注文住宅の請負会社の「LINE公式アカウント」公式事例は**存在しない**（＝先行者になれる）。
    近い事例は auka（ギバーテイクオール）／オープンハウスG／ミサワホーム北越／LIFULL HOME'S。

- 2026-08-26: クラウド環境（claude.ai/code）の落とし穴。
  - `libreoffice-impress` / `libreoffice-calc` が未導入だと PPTX/XLSX を開けず QA できない。
    `apt-get install -y libreoffice-impress libreoffice-calc` で入る。
  - `pdftoppm`（poppler）も無い。`_build/qa_render.py` は PyMuPDF へ自動フォールバックする。
  - メイリオが無いため QA画像は代替フォント。**文字あふれの最終確認は必ずPCの PowerPoint で。**

- 2026-08-27: 賃貸業界（業界汎用）を2本立てで設計。**スライド作成プロンプトまで完成、PPTXはまだ。**
  - Ver.A＝入居者集客（B2C／仲介＋管理併営／主KPI＝CPO）、
    Ver.B＝オーナー開拓（B2B／管理受託主／主KPI＝面談CPA・CVR）。各36枚。
  - 引き継ぎはこの順で読ませる：
    `_drafts/賃貸業界_アジェンダ2本立て.md` → `_drafts/賃貸業界_要旨2本立て.md`
    → `_drafts/賃貸業界_調査結果.md` → `_drafts/賃貸業界_Ver{A,B}_スライド作成プロンプト.md`
  - **業界汎用なので成果シミュレーション（SIM）は入れない**（殿村さん指示）。広告の話も
    Ver.A の S06 だけに留める。
  - データは実測が入っている：Googleトレンドの生CSVは `_data/trends/`、
    LINEヤフーの前後検索（Journey）の分析は調査結果.md の §8-9。
    **KWは必ず一語**（二語にすると検索Volが落ちて使えない）。「内見」は測定不能で不採用。
  - 効いた発見：賃貸の検索は**年中フラット**（ピークは7月）。なのに契約は1-3月に集中＝
    業界は1-3月しか刈っていない、という論に組み替えた。

- 2026-08-27: PPTX貼り込み用の画像生成スクリプト `_build/make_images.py` を追加。
  - `python3 _build/make_images.py [trend|phone|menu|funnel]`。出力は `_images/`。
  - Googleトレンド折れ線／LINEトーク画面モック／リッチメニュー／ファネル図の4種。
  - **Googleトレンドは相対指標。スケールの違うKWを1枚に混ぜると小さい方が0に潰れる**ので
    必ず別グラフにする（土地活用・アパート経営が確定申告に潰された事故があった）。
  - **系列色にDYMのネイビー`1F285A`・炭黒`333333`は使えない**（暗すぎ・彩度不足で
    配色バリデータがFAIL）。系列は `3467B2 / D9661F / 00897B / 8E4EC6`（全項目PASS、
    CVD ΔE 10.9）。文字・軸・注釈はブランドのインク色のままなので見た目は揃う。

- 2026-09-01: マルチAI分業ワークフローを構築（スキル `.claude/skills/multi-ai-relay/`）。
  - 狙い：指示出しはスマホで（歩きながら案が浮かぶタイプ）、資料の最終確認はPCで。
    検索・分析・要約＝**Gemini**、画像生成・進め方の洗い出し・情報収集（サイト/論文）・
    専門的見解＝**ChatGPT**、統合・実行（資料/SIM作成）＝**Claude**、で役割固定。
  - API連携ではなく**殿村さんが手動でコピペリレー**する運用。Claudeは
    「次に投げるプロンプトを完成形で作る」「戻ってきた出力を評価する」「最終的に資料に
    落とす」の3役。詳細フローは `multi-ai-relay/SKILL.md` 参照。
  - 決まった細部：①Gemini出力は要約せず**全文コピペ**で戻してもらう
    ②画像生成プロンプトは**スライド構成が固まってから一括**で作る（都度は作らない）
    ③このワークフローは**専用スキル**として定着させる（都度会話で即興にしない）。
  - セッション共有は「同一セッションを端末間で継続」＋「別セッション間の文脈共有」の
    **両方**を要望されている。前者は claude.ai/code で同じセッションURLを開けば済むだけ。
    後者は自動化できないので、案件が一区切りついたら
    `_drafts/<案件名>_要件定義（引き継ぎ用）.md` に要点をまとめて、この記憶メモに
    リンクを残す運用を徹底する（今のところこれが唯一の方法）。

- 2026-09-01: **「おもちゃ箱」＝ `_memory/INDEX.md` を新設**（セッション間の記憶問題への回答）。
  - きっかけ：「カフェのSIM」を探したが全ブランチに存在せず、殿村さん自身もどのセッションで
    作ったか覚えていなかった。原因は**セッション内で作って push せずに終わった**こと。
  - **セッション同士は直接記憶を共有できない**（別コンテナで隔離。他セッションに指示を
    送ることも権限的に不可＝実際に試して失敗）。共有できるのは「共通の置き場」経由だけ。
    - 殿村さんが「同期できている」と言った *タスク管理シート* と *業務報告_自動化* も、
      会話が同期していたのではなく**両方が同じGoogleスプレッドシートを触っていた**だけ。
      原理はこの `_memory/INDEX.md` 運用と同じ。
  - 仕組み：`.claude/hooks/session-start.sh`（SessionStartフック）が
    セッション開始時に `_memory/INDEX.md` を自動で読み込ませる。設定は `.claude/settings.json`。
    → **どの端末・どのセッションから始めても、最初から全案件の地図を持った状態で始まる。**
  - **判明した重要事実：成果物がブランチに散らばっていて master に統合されていない。**
    EC業界PPTX・賃貸VerA/VerB PPTX・小林住宅などは作業ブランチにしか無い
    （最も揃っているのは `claude/pptx-creation-revision-5a4v0d`）。対応表はINDEXにある。
  - **記憶から漏れていた案件を発見**：EC業界・単品通販業界（2026-08-27作成）はこの記憶メモに
    記載が無かった。INDEXに収容済み。
  - 鉄則：**セッション内で作ったファイルは commit & push しないと消える。**

- 2026-09-01: **散らばっていた成果物88ファイルを1ブランチに集約した。**
  - 同名で複数版あるものは「そのファイルの最終更新が最も新しいブランチ」を機械的に採用。
    賃貸VerA/VerBのPPTX・チェックリストは `pptx-creation-revision-5a4v0d` の8/31版を採用
    （`kochira-douzo-mzomvp` の8/28版は履歴に残っているので必要なら取り出せる）。
  - 集約して分かったこと：
    - **`lineoa-sim` / `lineoa-sim-edit` / `kizon-anken` スキルが別ブランチに存在した**
      （このメモには「lineoa-sim は未設置」と書いてあったが誤り）。**カフェSIMはこれで作れる。**
    - **人材（転職）業界**の案件（アジェンダ2本立て＋原稿15枚）も記憶メモから漏れていた。PPTXは未作成。
    - `_learn/` にWebマーケの学習教材一式（ポッドキャスト台本10本＋player.html）があった。
    - プリントアース案件の資料が4ブランチに分散していた。
  - 以後、成果物・下書き・スキルは全部このリポジトリの1ブランチに置く。散らさない。

- 2026-09-02: **「他のチャットと同期を」→ 全ブランチを走査して統合した。**
  - 見つかった事故：`origin/master` をマージした時に `_memory/INDEX.md` の09-02追記
    （古河林業・ニナファーム・小林住宅ステイ・ノースト）が**消えていた**。復元済み。
    **INDEX/CLAUDE.md はマージで片側が丸ごと採られやすい。マージ後は必ず行数を見比べること。**
  - 取り込んだ成果物：カフェSIM＋引き継ぎメモ（未完#1解消）／テン・エンタープライズ提案24枚＋SIM ver2／
    八十吉（エイトビート）一式26ファイル／人材（転職）要旨／プリントアース企画案の最新版／`_build/README.md`。
  - CLAUDE.md の記憶メモも、各ブランチにしか無かった分を下の「他ブランチから統合」セクションに集めた。
  - ⚠️ **社内数字がリポジトリに入っている**：`20260831_案件一覧_部署集計_11列.xlsx`＋`.tsv` 2本
    （売上・粗利・担当を含む）。`proposal-management-sheet` ブランチでは「コミットしない」と決めて
    .gitignore に入れていたが、9/1の集約で master に入ってしまった。.gitignore は今回統合したが、
    **追跡済みファイルの除外（`git rm --cached`）は殿村さんの判断待ち**。

- 2026-09-04: **「オレ専用のAI」＝Webチャット画面で作った（殿村の相棒）。**
  - 選択肢4つ（LINEで話す／Claude Code強化／Webチャット／お客さん向け商材）から
    殿村さんが「Webのチャット画面」を選択。
  - 実体は claude.ai の Artifact（`_app/aibou/index.html`）。サーバー・APIキー不要。
    AI呼び出しは `sample`、記憶は `db`（端末間共有）。URLと手順は `_app/aibou/README.md`。
  - 中に入れた人格＝CLAUDE.md の「ユーザーについて」「禁止事項」＋INDEXの要約。
    画面の設定から書き換え可能（db に保存されるので再公開で消えない）。
  - **更新するときは新規公開ではなく同じURLへ再公開する**（Artifact の `url` 引数）。
  - 未確認：スマホ実機での表示（クラウドのヘッドレスChromeは最小ウィンドウ幅の都合で
    390px幅を正しく撮れなかった）。崩れていたら殿村さんに言ってもらう。
  - **決定（殿村さん）：APIキー版（Claude API／Gemini／OpenAI）は作らない。Web版のまま使う。**
    使い方は「URLをスマホのホーム画面に追加」して、Claudeのチャットを開かずに直接使う。
    claude.ai へのログインだけは必要（頭脳がそこにあるため）。今後この話題で API 案を再提案しない。
  - **その後「ログインしなくても使えるように。記憶は要らない」→ 私用PCでやる、と決定。**
    ログイン不要＝APIキーが要る（頭脳をどこかで動かす必要があるため。記憶の有無は無関係）。
    → `_app/aibou-gas/`（GAS＋Gemini無料キー・記憶なし・合言葉つき）を用意。
    立ち上げは**私用の Google アカウント**で行う。手順は同フォルダの README。
    クラウドから Gemini API に届かないので**実機未検証**。エラー文を貼ってもらって直す。
  - **2026-09-04 私用PCで立ち上げ完了（動作確認済み）。** 私用Gmail（ryota2003…）のGAS。
    続けて「情報の足し方」を聞かれ「シートで」→ `Code.gs` をシート対応版に更新。
    スプレッドシート「殿村の相棒メモ」（`setup` が自動作成）の「設定」「メモ」シートを相棒が毎回読む。
    チャットで「覚えて ○○」と送るとメモシートに追記。**この相棒の記憶とCLAUDE.md/INDEXは自動同期しない。**
    殿村さんから「相棒の案件地図を出して」と言われたら、INDEXから `CASES` 用の要約文を作って渡す。

## 記憶メモ（他ブランチから統合・2026-09-02）

> 2026-09-02 に「他のチャットと同期を」の指示で、各作業ブランチの CLAUDE.md に散らばっていた記憶メモを
> このセクションに集めた。**セッション内で CLAUDE.md に書いたら、必ずこのブランチ（→master）に push すること。**
> 元のブランチ名を各ブロックの先頭に残している。

<!-- from claude/excel-editing-g3wxlc -->
- 2026-08-25: 月次管理ブックの営業数字を CRM スプシに取り込む件。**GASで解決済み**。
  - 依頼元は佐村さん。最初は「商材カテゴリ×契約形態×売上×粗利×担当を1シートで」
    だったが、やりとりの中で**要件が3回変わった**。最終形は以下。
  - **最終形**：CRMスプシ（`1B3DEUyyBnFO1AQvVZUQL-QmLqrzRe5NHngaLhu04_s0`）に
    GASを入れ、メニュー「案件データ」→「最新に更新」で
    `【自動】ストック` `【自動】ショット` の2タブを生成する。
    → `_build/crm_sync.gs`。これを CRM の Apps Script に貼るだけ。
  - 1行=1エンドクライアント。金額はその契約形態ぶんのみ。両方持ちの会社は
    両タブに出て、「もう一方の契約形態」列で取りこぼしを防ぐ。
  - **要件が変わった経緯（同じ失敗を繰り返さないため）**：
    1. 集計表を作ったら「担当がわからない、エンド名がないと既存アタックできない」。
       → 依頼の言葉（商材カテゴリ×…）ではなく**目的（既存アタック）**を先に確認すべきだった。
    2. 「全体シートに追加するの怖いからやめて」→ 月次管理ブックには触らない。
    3. 「CRMのスプシに移行して」「ストックとショットは別々で」→ 現在の形。
  - **元データの列の並びは媒体ごとに違う**。位置ではなく**見出し名で解決**すること。
    AD等は H/I/J/K/P、メディアは K/L/M/N/F、ベトナムとマス広告は商材が N 列。
    担当の列名も ASP だけ「広告主担当①②／媒体担当①②」と違う。
  - **Claude はスプシに直接アクセスできない**（docs.google.com がegressブロック）。
    だから GAS にした。ファイルの受け渡しも不要になり、21MBのやりとりが消えた。
  - 踏んだ落とし穴：
    - 件数の数え方を2箇所で変えると差分がマイナスになる。
      「計上種別が入っている案件」で統一する（請求額の有無で数えない）。
    - 契約形態を金額で判定すると、金額0のヨミ案件が「未設定」に落ちる。
      **計上種別そのもの**で判定すること。
    - 商材リストをハードコードすると新商材が漏れる。GAS版は集約キーに商材を
      使っていないので、この問題は起きない。
  - 補助スクリプト（xlsx出力。GASが使えない場面用）：
    `_build/build_attack_list.py`（エンド別／案件一覧／商材別の3シート）、
    `_build/build_crm_master.py`（ストック・ショット別のCRM移行マスタ）。
    どちらも `extract()` を共有。引数に月次管理ブックのパスを渡す。
  - 未確認のまま残っている点：MEOの原価がマイナス（粗利率100.2%）、
    CS・PR・タレントシェアに売上マイナスの行あり。返金・調整か入力ミスか要確認。
  - 生成物と元ブックは社内数字なので **リポジトリにコミットしない**（.gitignore 済み）。
    スクリプトは数式・ロジックだけなのでコミットしてよい。

<!-- from claude/special-plan-change-slide-d3rpsl -->
- 2026-08-26: 特別プランの提供方針（クライアント告知用）。**言い回しは確定版**。
  - 10月〜のプランではなく、**11月からのご案内**になる。
    （※「10月は原則ご提供不可」という書き方はしない。殿村さん修正済み）
  - 弊社からのご対応として、各社様の配信金額と媒体インセンティブの兼ね合いから
    特別プランの調整をいたします。
  - 告知スライド1枚を `20260826_特別プランのご提供方針.pptx` として作成。
    ビルドは `_build/build_special_plan.py`（FMTをコピー→1枚に削減→中身を作り直す経路）。
  - 資料の好みメモ：装飾は少なめ・1枚に要素を詰めない。

- 2026-08-26: 佐村さんと認識合わせした結果、**ストーリーはこれが確定版**。
  1. 課題：10月にLINEヤフー社の追加メッセージ単価が改定（20万通超は一律2.5円）
  2. リスク：一斉配信のままだとコストが増大（大量配信のボリューム割引が縮小するため）
  3. 解決策：セグメント配信への移行 ＋ 弊社特別プランの適用で最適化
  4. スケジュール：適用開始は10月ではなく**11月から**
     → **理由：10月は配信実績データの計測期間**。その実績と媒体インセンティブを
       踏まえて11月に各社様ごとの特別プランを個別調整するため。
  - トーンは ネガ（値上げ）→ ポジ（11月からの特別プラン）へ着地させる。
  - 成果物：`20260826_LINE料金改定へのご対応.pptx`（1枚・4ステップ）
    ビルドは `_build/build_line_price_revision.py`。引数なしでFMTから1枚もの、
    `<既存.pptx> [出力] [ページ番号]` を渡すと既存デッキの該当ページを差し替える。

- 2026-08-26: 社内と社外で言い方を分ける（重要）。
  - 社内（佐村さん向け）：10月は原価（インセンティブ着地・配信実額）が読めず
    利益割れリスクがあるので、特別プランの適用は原則見送りたい。
  - 社外（お客様向け）：10月は新料金での実データを計測する期間。その実績と
    媒体インセンティブを踏まえて、11月から貴社専用の特別プランを適用する。
  - **弊社側の事情は資料に一切書かない。** 「口実」等の表現は社内チャットにも残さない
    （転送事故の元）。社内は「なぜできないか」、社外は「何をするか」で話す。
  - スライドは3ステップ（10月〜／ご提案／11月〜）＋※行の1枚に着地。
    殿村さんの好み：文字を減らして大きく、装飾は少なめ。

- 2026-08-26: 「金額をわからないように」の意図は **横展開（他社への使い回し）** だった。
  → 特定の1箇所ではなく、**クライアント固有の数字を全部**伏せるのが正解。
  - ビルドは `_build/mask_amounts.py`。半透明の白をかぶせるのではなく
    **テキスト自体を差し替える**（かぶせるだけだとコピペで元の数字が読めるため）。
  - `python _build/mask_amounts.py <入力> <出力> <クライアント名> --skip=2`
    （--skip には媒体の公開料金だけのページを渡す。伏せると資料が成立しないので）
  - 伏せた：金額→¥--- ／ 通数→--- ／ 単価→¥-.-- ／ 還元率→〇% ／ 社名→〇〇
  - 残した：期間(YYYYMM)、¥0・--、LINEの公開料金（S2の\15,000、¥2.5-3.0 など）
  - クライアント資料そのものは Git にコミットしない（マスク前の数字が履歴に残るため）。
    スクリプトだけコミットする運用。

---

## 【確定】特別プランご案内スライドのフォーマット

2026-08-31 佐村さん承認済み。**この形が最終版**。以後これをベースにする。

### 採否

| 版 | 構成 | 結果 |
|---|---|---|
| 2ブロック版 | ご案内／弊社のご対応 | **採用** ← これ |
| 3ステップ版 | 10月〜／ご提案／11月〜 | 不採用 |
| 4ステップ版 | 課題／リスク／解決策／スケジュール | 不採用 |

こねくり回した結果、**一番シンプルな最初の版に戻った**。
迷ったら要素を足さずに減らす。殿村さんの好みも一貫してこれ。

### 成果物とビルド

- 成果物：`20260826_特別プランのご提供方針.pptx`（1枚）
- ビルド：`python _build/build_special_plan.py`（引数なし）
- 共有形式：**PDF 1ページ**に書き出して渡す
- 経路：`_templates/DYM_LINEOA_FMT.pptx` をコピー → 2枚目だけ残す →
  `clear_slide()` → 区切り線を複製し直して中身を組む
  （スライドの新規追加はしない。python-pptx で壊れるため）

### レイアウト実測値（スライド 10.83 × 7.50 inch）

| 要素 | 位置 (x, y, w, h) inch | 書式 |
|---|---|---|
| タイトル | 0.60, 0.13, 8.10, 0.40 | 16pt bold メイリオ `#002060` |
| 区切り線 | y=1.52（FMT 2枚目から複製） | — |
| カード1 | 0.50, 1.95, 9.83, 2.05 | 塗り `#F4F7FF` / 枠 `#D9D9D9` |
| チップ1 | 0.85, 2.73, 1.70, 0.50 | 塗り `#1F285A` / 14pt bold 白 |
| 本文1 | 2.75, 1.95, 7.40, 2.05 | 18pt bold `#333333` / 上下中央 |
| カード2 | 0.50, 4.45, 9.83, 2.05 | カード1と同じ |
| チップ2 | 0.85, 5.23, 1.70, 0.50 | 塗り `#1F285A` / 13pt bold 白 |
| 本文2 | 2.75, 4.45, 7.40, 2.05 | 18pt bold `#333333` / 行間1.25 / 2行 |

- 色：紺 `#1F285A`／タイトル紺 `#002060`／赤 `#C00000`／本文 `#333333`
  ／薄青 `#F4F7FF`／枠 `#D9D9D9`
- フォント：メイリオ（`a:ea` と `a:cs` にも typeface を必ず入れる）
- 赤字は「**11月から**」の4文字だけ。他は本文色。

### 掲載テキスト（確定文言・変えない）

- タイトル：特別プランのご提供方針
- 【ご案内】10月〜のプランではなく、**11月から**のご案内になります。
- 【弊社のご対応】各社様の配信金額と媒体インセンティブの兼ね合いから、

### 検品

Linux 側のレンダリングはメイリオがなく IPAゴシックで代替されるため、
文字幅がズレる。**最終確認は PowerPoint で開いて行う。**

```bash
# pptx スキルの soffice ラッパーを使う（パスは環境で変わるので find で引く）
SOFFICE=$(find /root/.claude/skills -path '*pptx/scripts/office/soffice.py' | head -1)
python3 "$SOFFICE" --headless --convert-to pdf <file>.pptx
pdftoppm -jpeg -r 150 <file>.pdf out
```

クラウド環境の初回は依存が入っていないことがある：
`pip install python-pptx defusedxml markitdown[pptx] lxml Pillow`
`apt-get update && apt-get install -y libreoffice-impress poppler-utils`
（libreoffice-core だけだと .pptx を読めず「source file could not be loaded」で落ちる）

<!-- from claude/proposal-management-sheet-f87u96 -->
- 2026-08-31: **★人が使っている共有シートには、頼まれるまで書き込まない。**
  提案管理シート（本番）に足した5列は、**佐村さんに消された**。
  アポリストのタブごと消えて、版履歴から戻す羽目になった。
  - 原因は僕（Claude）の側にもある。頼まれてもいない
    「行を足すGAS」「列を足すGAS」を先に作って渡し、殿村さんが
    それを実行できる状態にしてしまった。**「これは要りますか」を
    一度も聞いていない。**
  - **順番はこう。**
    1. まずファイル（xlsx / TSV）で渡す。相手が自分で貼るかどうかを決める
    2. 貼る場所・貼り方を相手が決めてから、必要ならスクリプトを作る
    3. 共有シートへの書き込みは、依頼された時だけ
  - 「元シートを触らない」と言われたら、**列を足すのも触ることに入る**と
    考える。安全なのは別ファイルで渡すことだけ。

- 2026-08-31: **★佐村さんが本当に欲しかったのは「1行=1案件の11列」だった。**
  Talknoteでの原文：
  > なんか完成したシートがイメージとぜんぜん違うんだけど
  > OA以外の別商材の案件一覧をただぺたぺたしてほしいだけなのだけど
  > 関数とかいらないんだよね…。
  > 部署集計の各商材の
  > 商材　クライアント名　エンドクライアント名　商材詳細　ストック売上
  > ストック利益　売上　利益　担当者1　担当者2　担当者3
  > を合わせてもらうだけ…。
  - **これで合っていると殿村さんに確認済み。** `_build/build_annken_list.py`。
    実測 **1,276件**（1,516件からアカウント貸し73件とOA167件を除外）／
    売上 18.2億・利益 1.67億／うちストック891件。
    出力は `20260831_案件一覧_部署集計_11列.xlsx` と同名の `.tsv`（見出し無し）。
  - ストック売上・ストック利益は**計上種別がストックの案件だけ**入れ、
    ショットは空欄。売上・利益は契約形態に関係なく全件入れる。
  - 担当者1〜3は アカウント／コンサル／運用①②③ から重複を除いて3人。
  - 「部署集計」＝月次管理ブックの媒体タブ（AD/アフィ/CS/…）のこと。
    `2026年8月_売上管理シート(部署集計)【極秘文書】` がそのブック。
  - **★ここまで遠回りした原因。** 「商材カテゴリ×契約形態×売上×粗利×担当」
    という言葉だけを受け取って、**集計表→エンド別サマリ→提案管理シートへの
    行追加→右に5列**と作り込んでいた。欲しかったのは**ただの明細の貼り付け**。
    → 列名を言われたら、**その並びのまま1行=1レコードで出す**のが先。
      集計・サマリ・数式・GASは、頼まれるまで作らない。
  - TSVはセル内のタブ・改行を潰すこと。元データに
    `ワンタグシステム利用料[タブ]_月額固定費` があり、列がずれた。

- 2026-08-31: **提案管理シートに実績5列を足す件。コピーで検証OK、本番へ。**
  - 対象は2つ。どちらも**アポリストのタブが gid=281377521**。
    - 本番 `18TLsRqtH34Bq4o7wYwWL57Zf8Z3TnRIb4khCmxwZKA0`
    - コピー `1IQObsH-aumtz4pZdifWucKQtV1InT_S5Rmb4OQP9X8k`
  - `_build/add_5cols_gs.gs`。P〜T列に
    `商材カテゴリ / 契約形態 / 実績売上 / 実績粗利 / 実績担当` を足す。
    A〜O列には書かない。行の挿入・削除・並べ替えもしない。
    メニューは ①ヒット状況（書き込まない）②5列を入れる ③入れた5列を消す。
  - 結果：**2,156行中242行がヒット（11%）**。残りは空欄。
    アポリストは「提案した先」、月次管理ブックは「請求が立った先」なので、
    受注していない会社は元データに存在しない。手持ちは795社なので上限もそこ。
  - **★同じ会社が複数行あると、全部の行に同じ実績が入る。**
    R列を単純にSUMすると多重計上になる。会社単位の合計が要るなら
    2件目以降を空欄にする版が要る（未実装）。
  - T列（実績担当）だけフルネーム。L/M列は名字なので揃っていない（未対応）。
  - **★大失敗：行を足すスクリプト（`teian_import_gs.gs`）を先に渡してしまい、
    殿村さんが実行して行が795行ずれた。** さらにエクセルを丸ごと貼ったせいで
    コピーの中身が置き換わった。**「行を動かしたくない」が要件だったのに、
    行を足す方向で作り込んでいたのが原因。**
    → 要件は「今あるデータは1ミリも動かさず、こちらから合わせる」。
      迷ったら列を右に足す方向で考える。
  - 殿村さんは長い説明と選択肢の羅列を嫌う。**手順は1本に絞って出す。**
  - **本番はまだ入っていない。** メニューを押すと
    「スクリプト add5cols でエラーが発生しました」。実行ログに onOpen しか
    残っておらず、関数が動く前に落ちている＝**認証が未了**。本番は
    コピーとは別のスクリプトプロジェクトなので、認証をやり直す必要がある。
    → エディタで `preview5cols`（書き込まない）を実行して認証を通す。

- 2026-08-31: **アカウント貸しは「外す」で決着。**
  - `build_attack_list.py --除外アカウント貸し`。サービス詳細が
    「アカウント貸し」の案件を集計から外す。
  - 実測：**73件・721,843,763円**を除外。795社 → **756社**（39社が消える）。
    売上 25.9億 → **18.7億**。
  - **★1位の株式会社サポート（3.1億）と3位の株式会社BHK（1.1億）が丸ごと消える。**
    アカウント貸しの案件しか無かったため。数字を見せる相手には先に言うこと。
  - 出力：`20260831_既存アタック用_案件一覧_アカウント貸し除外.xlsx`

- 2026-08-31: 既存アタックのリストを **LINEOA提案管理シート** に移管する件。
  - 対象シート：`1IQObsH-aumtz4pZdifWucKQtV1InT_S5Rmb4OQP9X8k`（gid=281377521）。
    見出しは1行目・**15列**：`提案 / 結論 / ☑ / 追客 / 企業名 / Ac名 / 既存 /
    商流 / 外注先 / 提案商材 / 業界 / 担当① / 担当② / DYM売上 / LINE売上`。
    最下部に `---` と「↑ここより上に行追加してください」の行がある。
  - **「この内容を変えずに移管したい」** が要件。既存の行・列はいじらない。
    → 途中まで GAS で行挿入（`_build/import_rows_gs.gs`）を作ったが、
    殿村さんの「送ってるデータに合うようにエクセルを改良すれば？」で方針変更。
    **エクセル側を15列の並びに組み替えて渡す**のが最終形。
  - `_build/build_teian_import.py`。`build_attack_list.extract()` を共有。
    出力は xlsx（15列＋参考列）と、見出し無しのTSV2本（貼り付け用／参考列）。
    `--除外アカウント貸し` でアカウント貸し案件を外せる。
  - **★列の意味を最初に間違えた。実データを見ないで名前から推測したのが原因。**
    殿村さんに「内容全然入ってないけど」と指摘されて直した。正しい意味は以下。
    | 列 | 中身（実データから読んだもの） |
    |---|---|
    | 提案 | 提案した**年月**。`23/12` `26/7` の形 |
    | 結論 | 日付／`NG`／`未提案`／`ー`／時間帯。統一されていない → 触らない |
    | ☑ | **カウント**（同じ会社が何回出ているか）。1〜4、`1755` は`<既存>`行の印 |
    | 企業名 | 会社名。`<既存>` `【リ】` `_ショット(-)` などの接頭辞を自分たちで付ける |
    | Ac名 | LINE公式アカウントの名前 |
    | 既存 | `●` `△` `● Indeed` `● clinic` |
    | 商流 | **いま経由している代理店の会社名そのもの**。直なら`直`、他に`不明``新規` |
    | 外注先 | **DYM側のパートナー**（エフ・コード／LINY／第一通信社／エルマーケ） |
    | 提案商材 | `商流変更`（最頻）／`コンサル`／`CPF`／`成果報酬`／`代理店`／`既存伸長（AD）` |
    | 業界 | 客の業種（飲食・アパレル・医療…）。DYMの商材ではない |
    | 担当①② | **名字だけ**。`奥田` `長谷川` `伊藤(優)`。フルネームでは入れない |
    | DYM売上/LINE売上 | **提案時の見込み額**。未提案の行（26/5〜26/7）は空欄 |
  - 新規行の最小形は 提案 + ☑ + 企業名 + 担当①② だけ（シート末尾の26/5〜26/7がそう）。
  - 埋めた列：提案＝今月／☑＝1／企業名＝エンド名／既存＝●／
    商流＝代理店名 or 直／提案商材＝商流変更／担当①②＝名字。
  - **空欄のままにした列**：結論・追客・Ac名・外注先・**業界**・DYM売上・LINE売上。
    実績データから出せない、または未提案だから。業界はシート側にあるのでVLOOKUPで引ける。
  - 実測：795社／商流は直571・代理店名あり217・不明7／アカウント貸しあり42社／
    実績売上 2,593,981,674（参考列にだけ入れる）。
  - 担当のフルネーム→名字は `SURNAMES` の辞書で最長一致。元データの
    `その他（AD）` `その他(CS)` は人名ではないがそのまま通している。
  - **アカウント貸しを外すかは未決**（73件・7.2億・売上の27.8%・粗利率0.76%）。
    消さずに参考列でフラグを立てる方針にしてある。

<!-- from claude/line-oa-greeting-message-znn29r -->
- 2026-08-31: **プリントアース（インクアート株式会社）のLINE OA改善**を開始。既存の提案
  デッキ案件とは別で、**運用中アカウントの実務改善**（成果報酬型／広告CPAと即時CVRの最適化）。
  - 引き継ぎは `_drafts/プリントアース_LINEOA_タスク1_あいさつメッセージ＆Flex.md` を読ませる。
    Flex実装は `_drafts/printearth_flex_greeting_carousel.json`（JSON検証済み・そのまま貼れる）。
  - 全4タスクのうち**タスク1（あいさつ＋初回オファー）まで完了（v2）**。残り：
    2=リッチメニュー改修案／3=デザイナー向け画像発注指示書／4=60分後〜当日夜のリマインド。
  - **ゴールの順序（殿村さん明言）：成果報酬で稼ぐのが最終目的だが、まずCVR/CPA改善、
    そのために企画・投稿案の作成から。** いきなり収益の話にしない。
  - **v2で構造が変わった（26年8月ADレポ／Gemini分析を反映）：客は2種類いる。**
    - 検索広告（Google/Yahoo!）＝BtoB・店舗販促。「格安印刷通販」「チラシ印刷」等。今すぐ安く。
    - Meta広告（IG/FB）＝BtoC・個人/クリエイター。推し活グッズ・透明グッズ・小ロット。
    - 現状のあいさつ（10万円クーポン・会員登録）は**検索層に「自分には関係ない」と思わせ、
      Meta層には法人臭でブロックされている**。これが離脱の主因。
  - **重要な制約：あいさつメッセージは標準機能で流入元別の出し分けができない**（全員一律）。
    → 本文は1通で両対応（法人臭を抜く）、分岐はカルーセル4枚（チラシ／推し活／見本帳／相談）。
    出し分けにはLステップ等の外部ツールかMessaging API連携が要る（中期の提案ネタ）。
  - **効いた一行**：「チラシ5万枚も、アクスタ1枚も、同じ工場で刷っています。」
    法人臭と個人お断り感を同時に消す。カード2の「個人の方のご注文も大歓迎です」も削らないこと。
  - **見本帳（紙サンプル）の無料請求が企画上いちばん効率が良い**。請求に会員登録が必要なので
    「請求→会員登録→ポイント発生→後日注文」が1本につながり、その場で買わない層をCVで拾える。
  - **未完の申し送り4件：**
    1. **リッチメニューのURLが `https://` 二重**でタップ時エラー。最優先で修正（提案書§0に手順）。
    2. **【最重要】1,000ptが初回注文から使えるか未確認。** 公式規約は「次回以降のご注文時に使用」。
       もし登録特典も同じ扱いなら**初回の支払額が1円も下がらず**、広告の「すぐ使える」と矛盾
       （カートで期待が外れて離脱＋景表法リスク）。文面は初回可/不可の両パターンを用意済み。
    3. 他の未確定数値：新規特典1,000ptか2,000ptか、「最大50%OFF」「1枚1.2円〜」の適用条件。
    4. **JSONの会員登録URL・画像はプレースホルダ**（`/regist/` と `/line/greeting_card*.jpg`）。
       他のURL（`/lineup/chirasi_flyer/` `/lineup/oshikatsu_goods/` `/guide/request-info/`
       `/contact/` `/download/`）は実在確認済み。ブランドカラー（暫定 `#E8380D`）も要差し替え。
  - **次に殿村さんからもらうデータ：8月の検索広告 vs Meta広告の友だち追加数の比率。**
    これでカルーセルの並び順とあいさつ案（A=ポイント型／C=BtoC寄せ）が確定する。

- 2026-08-31: プリントアース **9月の配信企画案8本＋初回接触の改修**まで完成。
  - 引き継ぎは `_drafts/プリントアース_LINEOA_配信企画案_2026-09.md`（現状診断・企画8本・
    リッチメニュー改修まで全部入り）。カレンダー貼り込み用は
    `プリントアース_LINEOA_配信企画案_2026-09.xlsx`（再生成は `_build/build_printearth_plan.py`）。
  - **殿村さん指定：企画ごとに「企画案／狙い概要／ターゲット／バナー・構成案／テキスト」は
    マスト。** xlsxもこの5項目を列として持たせてある。
  - **実画面スクショで見つけた致命的バグ：あいさつ本文の「👇アンケートはこちら」の直下に
    「新規会員登録」バナーが置かれている（矢印とバナーの中身が不一致）。** 最優先で修正。
    3つ目の吹き出しが「※個別メッセージは対応しておりません」単独＝最後に残る印象が最悪。
  - **現行リッチメニュー（2×3）の一等地（左上）が「テンプレDL」でCVから最も遠い。
    推し活・グッズの入口がゼロ**＝Meta流入層の受け皿がない。改修案は企画書§3。
    色・アイコン・角の三角アクセントは良いので**配置と項目だけ**作り直す。
  - **ブランドのトンマナ（過去CR素材から抽出）**：緑 `#0E8A4F`／CTA赤 `#E8564B`／
    橙 `#F0912D`／強調黄 `#FFE800`。配信画像は 1040×1040 px。
    定型は「白フレーム＋角の三角＋黄色い吹き出し＋緑の特大見出し」。
    キャンペーン系は「季節モチーフ背景＋左上に黄丸で割引率＋下部CTA帯＋右下に期限」。
  - **utm命名規則（既存カレンダー準拠）**：`utm_source=LINEOA` / `utm_medium=crm` /
    `utm_campaign=YYYYMMDD_message_XXXX`。Flex JSONもこれに揃え済み。
  - **確定した実URL**：会員登録 `/entry/regist/`、キャンペーン `/cp/renewal/`、
    見本帳 `/guide/request-info/`、同人誌 `/lineup/doujinshi-all/`、
    トレカ `/lineup/trading_card_all/`、カレンダー `/lineup/calendar/`、
    うちわ `/lineup/fan/`、シール `/lineup/sticker-print/`、推し活 `/lineup/oshikatsu_goods/`。
  - **提案の肝：全配信が「全員」なのをやめる。** 標準機能のクリックリターゲティングだけで
    BtoB（チラシ/名刺/カレンダーのクリック者）とBtoC（推し活/同人/トレカのクリック者）に
    分けられる。配信のたびにオーディエンスが育つので、3ヶ月で精度の高い2分割になる。
  - 過去CR素材の展開先（zip 5本）：`/tmp/.../scratchpad/cr/`。**セッションが切れると消える**
    ので、必要なら殿村さんに再送してもらうこと。
  - **【最重要の発見】オーディエンスが全部「インプレッション（見ただけ）」で、クリックが
    1つもない。** 絞り込みの精度がほぼゼロ。次の配信から**クリック**で作れば、管理画面の
    設定だけ（制作物ゼロ）でBtoB/BtoCが分かれ始める。同人誌のオーディエンスは10日以上
    「準備中」＝母数不足の疑い。→ **同人誌（冬コミ）の企画は見送りに変更（殿村さん判断）。**
  - **バナーを画像生成AIで作ろうとして失敗した。** 日本語が崩れるので「文字を入れるな」と
    指示する→空の枠しか出ない。**この用途に画像生成は使わないこと。**
    正解は過去バナーの**文字差し替え発注**（例：7/8の「最大10万円分」の見出しだけ
    「1,000円分のポイント」に差し替え、スタンプカード併用の一文は削除）。
  - **殿村さんの進め方の好み**：細部（トンマナ・画像）で止まるのを嫌う。
    **「トンマナは後で決める。先に文章を固める」**の順で進めること。
  - **`inkart.jp` はクラウド環境から egress ブロックで見られない**（page.line.me と同じ）。
    `curl` は403、`WebFetch` は `EGRESS_BLOCKED`。www有無どちらも同じ。
    プロキシの手引きに「迂回せず報告せよ」とあるので**回避策は取らないこと**。
    → **事実確認はPC側のClaude Codeでやる。手順は
    `_drafts/プリントアース_LINEOA_PC側で確認するタスク.md` に全部書いた（2026-08-31 殿村さん判断）。**
    確認が要るのは9項目。最優先は「ポイントが初回注文から使えるか」と「登録特典のpt数」。

<!-- from claude/pptx-creation-revision-5a4v0d -->
- 2026-08-31: EC業界・賃貸業界Ver.A/Ver.B（業界汎用モードB）を3本とも完成・納品済み。
  - `EC業界_LINEOA施策提案.pptx`（36枚・赤0黄7緑29）／
    `賃貸業界_LINEOA施策提案_VerA_入居者集客.pptx`（36枚・赤0黄2緑34）／
    `賃貸業界_LINEOA施策提案_VerB_オーナー開拓.pptx`（36枚・赤0黄2緑34）。
    各PDF・公表判断チェックリストxlsxも同梱。ビルドスクリプトは
    `_build/build_ec.py` / `build_chintai_verA.py` / `build_chintai_verB.py`。
  - **公表判断チェックリストの運用ルール：作成は続ける（必要性はある）が、
    チャットへは自動送付しない。殿村さんが「チェックリストも出して」等と
    明示的に求めたときだけ送る。**リポジトリへのコミットは通常どおり続けてよい。
  - EC版は前後検索・競合友だち数・LY事例が未取得のため差込枠が多い。
    賃貸Ver.A/Bは実データ（Googleトレンド・LINEヤフー前後検索）が取得済みで
    差込枠は競合の友だち数のみ。
  - **フォントの件（聞かれたら）**：`a:latin`（英数字）も`a:ea`（日本語）も
    "メイリオ"表記で統一で確定（殿村さん確認済み。"Meiryo"表記に分ける変更は不要）。
    クラウド環境にはメイリオの実体フォントが無いため、LibreOffice経由のPDF/PNGは
    代替フォントで見える。**PPTX自体は正しくメイリオ指定なので、PowerPointで開けば
    問題ない。** PDFを本物のメイリオにするには殿村さんのPCのPowerPointから
    書き出す以外に方法がない（このクラウド環境では不可能）。
  - **古河林業の資料修正（P20 全体設計）は保留中。** 対象のpptx・SIM xlsxが
    `C:\Users\...` のローカルパス指定だったが、ここはクラウド実行環境なので
    アクセス不可。**添付でアップロードしてもらうまで着手できない。**

<!-- from claude/handoff-sheet-redesign-0f8beu -->
- 2026-09-01: 案件引き継ぎ用スプレッドシートの構成案を作った。
  - **v1（`_drafts/案件引き継ぎシート_構成案.md` 初版）は実物未閲覧のまま作った仮案で破棄済み。**
    クラウド環境からは `docs.google.com` が egress ブロックで開けない（Sheets連携も未接続）。
  - **v2で作り直し。** 殿村さんが実物テンプレートの中身をチャットに貼ってくれたので、それを
    棚卸しして再設計した。実物は「1案件＝1タブ、■見出しのブロックが2列に並ぶフォーム形式」で、
    中身は完全にWeb広告（Google/YSA/YDA/Facebook/LINE広告/TikTok/X）特化、LINEOA項目はゼロ
    （背景説明の通り）。
  - 採用構造：**1案件1タブは維持**し、タブ内を【共通】→【Web広告】→【LINEOA(新設)】の3ブロックに
    整理（要件で提示された2案のうち低負荷な方）。
  - 続けて「GS方式で良いよ／コード書いて」と言われたので、この構成を自動構築するGoogle Apps
    Scriptを `_drafts/案件引き継ぎシート_GAS.gs` に実装した（`setupHandoffWorkbook` を実行すると
    「⓪設定」タブ＋「案件テンプレート」タブが生成され、以後は案件引き継ぎメニューから
    新規案件タブをワンクリック複製できる）。
  - 原本を見て気になった未確認点（構成案.md末尾に記載）：①「Lパターン」の意味不明
    ②先方担当者の番号が1・2・4で3が欠番。→ 殿村さんに確認待ち。
  - **その後「広告今回いらない」「ラインだけでいいよ」との指示でv3に更新。** Web広告ブロックを
    まるごと削除し、【共通】【LINEOA】の2ブロック構成に整理（GAS・構成案.md 両方を修正済み）。
    Web広告ブロック関連だった選択肢（指名広告可否・リマケ可否・広告文変更スパン等）も
    ⓪設定タブのマスタから削除した。広告ブロックが要るときは v2 の内容（git履歴）から復元する。

<!-- from claude/rich-menu-schedule-publish-k4jv2y -->
- 2026-09-01: リッチメニューの予約・公開の運用手順を `_drafts/リッチメニュー_予約公開の運用手順.md` に整理。
  - 管理画面のタブは「予約・公開」（公開中＋予約済み）と「待機中」（下書き・期間切れ）の2つ。
  - **同時公開は1つだけ。表示期間が重複すると保存できずエラー**になるので、
    必ず「先に今のを止める（待機中へ移す or 終了日時を切替時刻に変更）」→「後から新しいのを予約」の順。
  - **編集画面の最下部のボタンが全て**：白の**「下書き保存」→「待機中」**、緑の**「保存」→「予約・公開」**。
    詳細ページには「削除」「編集」しかないので、右上の「編集」から入ってから押す。
  - 公開中→待機中は「下書き保存」。ただし**移した瞬間にメニューバーごと消える**ので、
    空白を作りたくないなら終了日時の変更だけで繋ぐ手もある（2026-09-01は待機中に移す方針を採用）。
  - 予約公開は表示期間の開始日時を未来にして緑の「保存」。ステータスは「予約」→時刻で自動「公開中」。
  - 反映はLINEアプリのキャッシュで数分ラグ。確認は実機のトーク画面で。

<!-- from claude/operations-task-sync-1v4z8h -->
## 運用ルール（佐村さん依頼・全セッション共通）

- 2026-09-01 に佐村さんから依頼。**運用案件の進捗管理はこの5点を必ず満たす。**
  以降のセッションでは進捗表・報告物をこの形式で作ること。

  1. **ステータスを明記する。**
     `投稿案 → FIX → デザイン → 配信設定` のどの段階にいるかが一目でわかるように書く。
  2. **タスク進捗を「固定」と「変動」に分けて書く。**
     - 固定＝投稿数（毎月の定常運用分）
     - 変動＝ショット施策・イベント施策 等
     混ぜて1本の数字にしない。分けて見えるようにする。
  3. **ヨミを精査する。** 特に **Cヨミ** と **計上月** を必ず確認・更新する。
  4. **デイリー納品を徹底する。** 完成物だけでなく **途中物も毎日出す**。
  5. **既存運用領域の効率化がいつ進むのかを明確化する。**
     「何が削れていて」「何がショット的に増えているのか」を明示する。
     → **期限：9/1中に対応**（当日締め）。

<!-- from claude/industry-standard-materials-ndzvb9 -->
- 2026-09-01: **`_templates/DYM_LINEOA_FMT.pptx` の罠を記録**（FMTを流用する全案件に効く）。
  - 名前は「DYM汎用FMT」だが、**中身は人材(転職)業界の提案書が元**。36枚のうち11枚が
    そのままでは出せない：5,6＝グラフ未挿入の空ページ、7-10＝転職のGoogleトレンド／
    「あああ」「すうじ」等のダミー、12-16＝社内ブレスメモ。33-35＝転職者向けの設問・
    繁忙期カレンダー（バッジに「人材(転職)業界の例」）。
  - 5,6 は注文住宅メモの「S5/S6は削除候補」と同じ2枚。**汎用版では削除で確定させた。**
  - 業界を絞らない汎用版はこの11枚を外し、33-35を業種非依存の型に書き換えた25枚。
    再生成は `_build/build_generic_lineoa.py`、詳細は `_build/README.md`。

<!-- from claude/printearth-inkart-verification-tey119 -->
- 2026-08-31: **`inkart.jp` はクラウドから見られない**（egress ブロック）。
  `page.line.me` / `lineoa.jp` と同じ。WebFetch は `EGRESS_BLOCKED`、curl は
  CONNECT tunnel 403。TLSの問題ではなく環境の通信ポリシー（許可ドメイン制）なので
  こちら側に回避策はない（2026-09-01に再確認しても同じ）。
  **プリントアースの裏取りはPC側でやること**
  → `_drafts/プリントアース_LINEOA_PC側で確認するタスク.md`
  - ⚠️ このとき「プリントアース関連ファイルが一つも無い＝コンテナ破棄で消えた」と
    書いたが**誤りだった**。実際は別ブランチにあり、2026-09-01の集約でmasterに入っている。
    **作業ブランチが古いmaster基点だと「無い」ように見える。**
    ファイルを探すときは、まず `git fetch origin master` してから
    `git -c core.quotepath=false ls-tree -r --name-only origin/master` で見ること
    （日本語ファイル名は quotepath を切らないと grep に引っかからない）。

- 2026-09-01: プリントアースのLINE運用ルール。**月の配信数は4本**（殿村さん指摘）。
  企画は `_drafts/プリントアース_LINEOA_配信企画案_2026-09.md` に集約。
  - 「MAX 50%OFF」は**必ず「最大」を付ける**（有利誤認・景表法）。画像内の文字も同じ。
  - 配信画像はリッチメッセージの1040×1040。**日本語文字は画像生成AIに描かせない**
    （崩れる）。背景・商材だけ生成し、文字はCanva等で後乗せする。
  - 9/21(敬老の日)・9/23(秋分の日)で**9/21の週は実質3営業日**。駆け込みは連休前に前倒し。
  - ⚠️ **「10/1の人事異動で名刺」は仮説どまり。断定して書かないこと。**
    前後検索は「行動」しか映さず**動機はクエリに出ない**ため検証できない（否定もされていない）。
    さらにADレポで**名刺は広告出稿すらしていない**と判明した。
    → 動機を特定せず「秋冬に名刺を見直す」文脈で訴求すれば、異動にも年末挨拶にも当たる。

<!-- from claude/efficiency-measures-progress-mhmu3w -->
- 2026-09-01: **佐村さんから効率化施策の依頼。「進捗」と「期限設定」の2点を本日中に報告。**
  対象は次の2施策（求められているのは完成ではなく報告のみ）。
  1. デモ動作のAI化
  2. 個別管理票 → 全体管理表の連携
  - **重要な発見：②はゼロからではない。リポジトリに既に素材がある。**
    - `_drafts/案件引き継ぎシート_構成案.md`＋`案件引き継ぎシート_GAS.gs`
      ＝**これが「個別管理票」の統一フォーマット案（v3で確定済み）**。1案件1タブ、
      【共通】＋【LINEOA】の2ブロック、プルダウンは「⓪設定」タブに一元管理。
      未解決は2点だけ（「Lパターン」の意味が不明／先方担当者の番号が欠番）。
    - `_build/crm_sync.gs`＝月次管理ブック→CRMスプシに【自動】タブを生成（読むだけ・安全設計）。
    - `_build/teian_import_gs.gs`／`import_rows_gs.gs`＝月次管理ブック→LINEOA提案管理シートへ
      行追加（既存セルは1つも書き換えない設計）。
    - `task_management_sheet.gs`＝タスク管理シート（期日で色分け・毎朝カレンダー通知）。
    - `20260831_案件一覧_部署集計_11列.xlsx`／`_build/gyokai.py`（業界自動分類）。
    → つまり「全体→個別」方向は既に動く。**残っているのは「個別→全体」の集約方向だけ。**
      佐村さんへの進捗は「検討中」ではなく「**フォーマット統一案は確定済み・連携スクリプトは一部稼働中**」と書ける。
  - 提案した期限（3段階で切る方針。①範囲確定 → ②試作 → ③本運用）:
    - デモ動作のAI化: 9/4(金) 範囲確定 / 9/18(金) 試作＋社内チェック / 10/2(金) 本運用
    - 管理票の連携: 9/4(金) フォーマット統一案の確定 / 9/11(金) 試作 / 9/25(金) 本運用
  - **殿村さんへの確認待ち2点**（これが埋まると叩き台を作れる）:
    ① 「デモ動作」とは具体的に何のデモか（商談デモ／システム操作デモ／社内向け）
    ② 「個別管理票」は上記の案件引き継ぎシートのことか、それとも別物か
  - 殿村さんの認識（正しい）：「個別で管理せず全体で管理」＝入力は個別のまま、
    集約と閲覧を全体に寄せる。個別管理票は廃止しない（メンバーに運用変更を告げなくてよい）。
