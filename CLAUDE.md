# CLAUDE.md

このファイルは Claude がセッションを跨いで文脈を引き継ぐためのメモです。
セッション開始時に必ず読み込まれます。重要な決定事項や覚えておくべきことが
出てきたら、Claude はこのファイルの「記憶メモ」に追記してコミットすること。

## ユーザーについて

- 殿村亮太さん（DYM）。日本語で会話する。
- 返答は **短く・シンプルに・具体的な手順で**。長い解説や専門用語の羅列は好まない。
- セッションやデバイスを跨いでも「前に言ったこと」を覚えていてほしいという要望が強い。
  → だからこのファイルに要点を残す。会話で重要なことが決まったら必ずここに追記する。

## このリポジトリについて

- 営業提案資料（PPTX）の置き場 + 資料を自動生成するスクリプト群。
  - ルート直下: LINE公式アカウント活用提案などの完成デッキ（.pptx / .xlsx）
  - `_templates/`: デッキのベースとなるテンプレート PPTX
  - `_build/`: テンプレートから提案デッキを生成する Python スクリプト
    （`build_vivical_33.py`, `build_vivical_9p.py`, 検品用の `qa_render.py`）
- 認証情報類（*.json, .env など）は絶対にコミットしない（.gitignore 参照）。

## 記憶メモ（会話を跨いで覚えておくこと）

- 2026-08-25: 月次管理ブック（130シートのExcel）から営業数字のサマリを作る件。
  - 依頼元は佐村さん。「商材カテゴリ×契約形態×売上×粗利×担当」を1シートで見たい。
  - **元シートは絶対に触らない。別ファイルで新規作成する**（これが最優先の制約）。
  - **最終仕様（佐村さんの追加指示で確定）**：
    - 対象は **全13媒体シート**（AD／アフィ／CS／MEO／制作／PR／風評／タレントシェア／
      LINE公式アカウント／メディア／ベトナム／ASP／マス広告(MA)・その他）。
    - **担当別の数字は不要**。案件全体の数字だけでよい（＝按分列もインセン列も使わない）。
    - 対象月は全月合算。納品形態はスプシ内の新規タブ。
  - 列の対応：商材カテゴリ＝`商材` / 契約形態＝`計上種別`（ストック・ショット） /
    売上＝`請求額（税抜）` / 粗利＝`利益`。
    列の並びは**媒体ごとに違う**。AD等は H/I/J/K/P だが、
    メディアは K/L/M/N/F、ベトナムとマス広告は商材が N 列。要注意。
  - **Claude はスプシに直接アクセスできない**（docs.google.com がegressブロック）。
    「数式を書く→殿村さんが新規タブに貼る」で進めるしかない。
  - 貼り付け用ブロック生成：`_build/make_gsheet_block.py` → `_build/zen_baitai_shukei.tsv`。
    新規タブの A1 に貼るだけ。①媒体別サマリ（検算）②契約形態別 ③媒体×商材×契約形態 の3ブロック。
  - **踏んだ落とし穴**：①と③で件数の数え方が違うと差分がマイナスになる。
    両方とも「計上種別が入っている案件」で数えること（請求額の有無で数えない）。
  - **商材リストはハードコード**。実データが増えると新商材が漏れて「うち未分類」に出る。
    差分が出たら各シートの商材を `=SORT(UNIQUE(FILTER(...)))` で洗い直して行を足す。
  - 2026-08-25 時点の状況：貼り付け済み。全1,869件・売上約26.4億・粗利約2.1億。
    ストックの粗利率14.9% vs ショット1.8%。未分類14件（CS 2件・制作 12件）が残っており、
    商材名が分かり次第 MEDIA の cats に追記する。**佐村さんの確認待ち**。
  - 数字を見て気になった点（未確認）：MEOの原価がマイナス（粗利率100.2%）、
    CS・PR・タレントシェアに売上マイナスの行あり。返金・調整か入力ミスか要確認。
  - 別ファイル版（xlsx）の生成：`_build/build_ad_summary.py`（AD単体・担当別あり版。今は未使用）。
  - 生成物と元ブックは社内数字なので **リポジトリにコミットしない**（.gitignore 済み）。
    スクリプトと貼り付け用TSVは数式だけなのでコミットしてよい。

- 2026-08-24: クロスデバイスの記憶について整理した。
  - claude.ai アプリ（スマホ/デスクトップ/Web）の会話は「メモリ機能」で
    アカウント単位に自動共有される（設定 → 機能 → メモリ。反映に最大1日ラグ）。
  - Claude Code のセッションは自動同期されないため、覚えておくべきことは
    この CLAUDE.md に書いてコミットする、というのがこのリポジトリでの運用。
